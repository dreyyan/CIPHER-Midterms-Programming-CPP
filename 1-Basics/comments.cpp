// This is a single-line comment. 
// It only takes up one line, and the compiler ignores it.

/* 
   This is a multi-line comment. 
   It can span across several lines, and is also ignored by the compiler.
*/

#include <iostream> // Lets us use input/output (like printing text to the screen)

int main() {
    // The line below is "commented out," so it will NOT run.
    // If you remove the // at the start, it will print a message.
    // std::cout << "This code does absolutely nothing!" << std::endl;

    return 0; // End the program
}